{{/* Web IngressClass - Only one needed for web services */}}
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  labels:
    app.kubernetes.io/name: LoadBalancerController
  name: alb-web
spec:
  controller: ingress.k8s.aws/alb