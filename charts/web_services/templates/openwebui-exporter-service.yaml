apiVersion: v1
kind: Service
metadata:
  name: openwebui-exporter-service
  namespace: {{ .Values.global.namespace | default "web-services" }}
spec:
  ports:
    - port: 9090
      targetPort: 9090
      name: http-metrics
  selector:
    app: openwebui-exporter
