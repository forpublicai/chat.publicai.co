apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.app.name }}
  namespace: {{ .Values.global.namespace | default "web-services" }}
spec:
  replicas: {{ .Values.app.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.app.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.app.name }}
      {{- if .Values.prometheus.enabled }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "4000"
        prometheus.io/path: "/metrics"
      {{- end }}
    spec:
      {{- if .Values.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.serviceAccount.name }}
      {{- end }}
      containers:
        - name: {{ .Values.app.name }}
          image: {{ .Values.app.image.repository }}:{{ .Values.app.image.tag }}
          imagePullPolicy: {{ .Values.app.image.pullPolicy }}
          args:
            - "--config"
            - "/app/config.yaml"
          env:
            - name: AWS_REGION
              value: {{ .Values.env.aws_region | quote }}
            - name: LITELLM_MODE
              value: "PRODUCTION"
            - name: LITELLM_SALT_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: litellm_salt_key
            - name: USE_PRISMA_MIGRATE
              value: "True"
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: litellm_master_key
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: database_url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: redis_url
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: redis_host
            - name: REDIS_PORT
              value: "{{ .Values.env.litellmRedisPort }}"
            - name: LITELLM_REDIS_PORT
              value: "{{ .Values.env.litellmRedisPort }}"
            - name: SEALION_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: sealion_api_key
                  optional: true
            - name: VLLM_API_KEY_INTEL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: vllm_api_key_intel
                  optional: true
            {{- if .Values.lago.enabled }}
            - name: LAGO_API_BASE
              value: "http://platform-api-svc.platform.svc.cluster.local:3000"
            - name: LAGO_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: lago_api_key
                  optional: true
            - name: LAGO_API_EVENT_CODE
              value: "{{ .Values.lago.eventCode }}"
            - name: LAGO_API_CHARGE_BY
              value: "{{ .Values.lago.chargeBy }}"
            {{- end }}
            - name: INFOMANIAK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: infomaniak_api_key
                  optional: true
            - name: BIELIK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: bielik_api_key
                  optional: true
            - name: CSCS_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: cscs_api_key
                  optional: true


          volumeMounts:
            - name: config-volume
              mountPath: /app/config.yaml
              subPath: config.yaml
              readOnly: true
            - name: custom-auth-volume
              mountPath: /app/custom_auth.py
              subPath: custom_auth.py
              readOnly: true
            - name: custom-lago-callback-volume
              mountPath: /app/custom_lago_callback.py
              subPath: custom_lago_callback.py
              readOnly: true
          ports:
            - containerPort: {{ .Values.app.port }}
          resources:
            {{- toYaml .Values.app.resources | nindent 12 }}
      volumes:
        - name: config-volume
          configMap:
            name: {{ .Values.app.name }}-config
        - name: custom-auth-volume
          configMap:
            name: {{ .Values.app.name }}-custom-auth
        - name: custom-lago-callback-volume
          configMap:
            name: {{ .Values.app.name }}-custom-lago-callback