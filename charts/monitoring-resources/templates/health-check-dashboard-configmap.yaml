{{- if .Values.healthcheck.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: health-check-dashboard-configmap
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  health-check.json: |-
{{ .Files.Get "dashboards/health-check.json" | indent 4 }}
{{- end }}
