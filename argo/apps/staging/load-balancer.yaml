apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: load-balancer-staging
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: 'https://github.com/forpublicai/chat.publicai.co'
    targetRevision: dev
    path: charts/load-balancer
    helm:
      valueFiles:
        - values.yaml
        - ../../argo/environments/staging/load-balancer-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
